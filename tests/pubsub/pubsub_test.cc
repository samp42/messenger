#include "gtest/gtest.h"
#include "src/util/pub_sub/publisher.h"
#include "src/util/pub_sub/subscriber.h"
#include "src/util/pub_sub/subscriber_log.h"
#include "src/util/pub_sub/subscription.h"
#include <string>

namespace {

GTEST_TEST( PubSub_Test, Subscribe_Test ) {
    // test subscription mechanism to be working properly


};

GTEST_TEST( PubSub_Test, Subscribe_Test ) {
    // test that events are queued correctly


};

GTEST_TEST( PubSub_Test, Subscribe_Test ) {
    // test that event_queue is emptied and callbacks are executed when calling it periodically


};

} // namespace 
